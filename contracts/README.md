# Archived football-duel contracts

This directory contains an older football-duel prototype. It is not the
Reality Bridge submission and has no deployment instructions. The current
GenLayer application is maintained in [`apps/reality-bridge`](../apps/reality-bridge).

For the supported product, use the contract source, manifest, and QA steps in
that directory. Do not use the prototype files here for a deployment.
